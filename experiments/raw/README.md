# Raw artifact indexes

Each `<run_id>.json` file points to one immutable external
`work-online-merge-*` directory and records its manifest hashes.  Large raw
artifacts are intentionally not copied into Git.
