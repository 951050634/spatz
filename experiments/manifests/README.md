# Run manifests

Paper-facing manifest snapshots are written here only after a run completes.
The authoritative, incrementally updated manifest remains in the external raw
result directory so a timeout cannot erase partial evidence.
